services:
  redis:
    image: redis:7-alpine
    container_name: arxio-redis
    ports:
      - "6380:6379"
    volumes:
      - arxio_redis_data:/data
    restart: unless-stopped

volumes:
  arxio_redis_data:
