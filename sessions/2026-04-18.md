# Session — 2026-04-18

## Completed

### Architecture Decision (Session 1)
- AI JSON saved directly to MongoDB (`workspace.analysis`)
- No DOCX/PDF files — frontend renders JSON sections as in-app views
- Only PPT (.pptx) is a binary file — 15-slide + 5-slide saved to disk
- Export = pptx | quick_pptx | zip only

### Phase 3 + 4 — PDF Pipeline (Session 1)
- `workspace.analysis` — full AI JSON, all sections available to frontend
- `pptxPath` + `quickPptxPath` — disk paths to generated PPT files
- `processingStep` + `processingPercent` — written to MongoDB + Redis on each Celery step
- `GET /pdf/:id/status` — returns `{ status, step, percent }` from Redis (fallback to DB on completed/failed)
- Usage counter — `pdfs_used_this_month` incremented on upload
- Celery task — 8 progress steps: 10% → 20% → 35% → 45% → 65% → 80% → 90% → 100%

### Session 2 — All Backend Modules

#### Chat
- `POST /chat/message` — sends message, calls AI with full analysis as context, saves both messages, increments usage
- `GET /chat/{workspace_id}/history` — paginated chat history
- Scholar plan uses RAG chunks instead of full analysis JSON

#### Research Mode
- `POST /research/generate` — Tavily web search → AI synthesis → PPT generation via Celery
- `GET /research/{id}/status` — live step/percent from Redis, fallback DB
- `GET /research/{id}` — full research doc
- `GET /research/` — paginated list
- `GET /export/research/{id}/{fmt}` — pptx | quick_pptx | zip

#### Library
- `GET /library/` — merges PDF workspaces + research jobs, sorted by createdAt
- `POST /library/search` — regex search across originalName + topic

#### RAG (Scholar plan only)
- Qdrant Cloud — `arxio_papers` collection, `sa-east-1` cluster
- `index_workspace()` — chunks text (1500 chars, 200 overlap) → OpenAI embeddings → Qdrant upsert
- `search_workspace()` — embed query → cosine search → top 6 chunks
- PDF task step 60% — indexes after AI analysis, Scholar plan only
- Chat — Scholar uses RAG chunks, fallback to full JSON if Qdrant fails
- Fixed: `workspace_id` payload index required for filtered search
- Fixed: `query_points` API (qdrant-client v1.17), `FilterSelector` for delete

#### Subscription
- `GET /subscription/plans` — public, no auth, pricing page ready
- `GET /subscription/` — current plan + usage + limits
- `POST /subscription/activate` — access key + plan → upgrade user
- Mock payment: `SUBSCRIPTION_ACCESS_KEY=arxio-early-access-2026` in .env

#### AI Routing
- Groq removed completely
- OpenAI only: free/student → `gpt-4o-mini`, pro/scholar → `gpt-4o`
- Fixed: `max_tokens 8000 → 16000` (JSON was truncating at 15 slides + Q&A)

#### Plans
- Added `tagline`, `features[]`, `highlight`, `currency`, `billing` to each plan
- `excel` removed (generator not built)
- `seed_plans()` uses `$set` — DB always syncs from `plan.py` on restart
- `GET /subscription/plans` returns full frontend-ready plan objects

### Bugs Fixed
- Status endpoint returning 90% after completion — now reads DB directly for completed/failed
- Same fix applied to research status service
- `generate_quick_pptx` fallback to `slides[:5]` for research (no `quickSlides` field)
- PPT title slide — handles both `paperTitle` (PDF) and `title` (research)

## Current State
- Server: `./start.sh --server`
- Client: `./start.sh --client`
- All: `./start.sh --all`
- Redis via Docker: `docker-compose.dev.yml` (port 6380)
- 33 routes total, all tested

## API Summary
- `POST /pdf/upload` → `POST /pdf/:id/process` → poll `GET /pdf/:id/status` → `GET /pdf/:id`
- `POST /research/generate` → poll `GET /research/:id/status` → `GET /research/:id`
- `POST /chat/message` + `GET /chat/:workspaceId/history`
- `GET /library/` + `POST /library/search`
- `GET /export/:id/:fmt` + `GET /export/research/:id/:fmt`
- `GET /subscription/plans` (public) + `GET /subscription/` + `POST /subscription/activate`

## What frontend gets from API
- `GET /pdf/:id` → full workspace JSON including `analysis` (20 sections)
- `GET /pdf/:id/status` → `{ status, step, percent }` — poll during processing
- `GET /export/:id/pptx` → download 15-slide PPTX
- `GET /export/:id/quick_pptx` → download 5-slide PPTX
- `GET /export/:id/zip` → zip of both PPTs
- `GET /subscription/plans` → 4 plans with features, pricing, highlight flag

## Prompt Quality
- All 15 Ayesha files covered in JSON: overview, keyTerms, methodology, results, criticalAnalysis, slides, qaPrep, presentationScript, cheatSheet, flashcards (15 cards), furtherReading (5-7 items), technicalReference, literatureConnections
- Smart extraction: head (20K) + middle-from-28% + tail (4K) = 72K chars
- Key numbers (98.2%, 96.5%, 23%) confirmed present in extracted content

## Next — Frontend (Next.js 14)
Screens to build:
1. Landing page (`/`) — SSR, hero, pricing section, two CTAs
2. Auth (`/login`, `/signup`)
3. Dashboard (`/dashboard`) — drag-and-drop upload, processing screen with live progress
4. Workspace (`/workspace/[id]`) — sidebar with output list, preview pane, chat bar
5. Presentation mode — full-screen slide viewer with speaker notes
6. Research mode (`/research`) — topic input, results
7. Library (`/library`) — grid of PDFs + research jobs
